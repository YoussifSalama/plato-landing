# Plato Marketing Website - Environment Variables (Next.js)
# Copy this file to .env.local and fill in the values.

# ── PostgreSQL ──────────────────────────────────────────────
# Connection string for Drizzle ORM (required for Book a Demo & contact form)
DATABASE_URL=

# ── Public (client-side) variables ──────────────────────────
# These are exposed to the browser. Use NEXT_PUBLIC_ prefix.

# Employer portal URL (where "Hire Talent" / login buttons link to)
NEXT_PUBLIC_EMPLOYER_APP_URL=https://platohiring.com

# Job seeker portal URL (where "Find Jobs" / "Upload Resume" buttons link to)
NEXT_PUBLIC_APPLICANT_APP_URL=https://applicant.platohiring.com

# Fallback email shown when demo booking system is unavailable
NEXT_PUBLIC_DEMO_EMAIL_FALLBACK=hello@platohiring.com

# LinkedIn company page URL (shown in footer)
NEXT_PUBLIC_LINKEDIN_URL=https://www.linkedin.com/company/aere-capital/

# ── Integrations (server-side only) ────────────────────────
# On Replit, SendGrid and Google Calendar are managed via
# Replit Integrations (no manual keys needed).
#
# If hosting elsewhere, you will need:
# - A SendGrid API key for booking confirmation & contact form emails
# - Google service account credentials for Calendar event creation
